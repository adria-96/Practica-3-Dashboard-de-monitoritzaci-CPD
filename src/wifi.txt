Pas 1.1 (Llibreria i dades): A dalt de tot del fitxer:


#include <WiFi.h>
const char* ssid = "ProjectesSMX";
const char* password = "Hola1234";


Pas 1.2 (Iniciar WiFi): Dins del setup():
Serial.begin(115200);
WiFi.begin(ssid, password);
Serial.print("Connectant...");




Pas 1.3 (L'espera): Just a sota, al mateix setup():

while (WiFi.status() != WL_CONNECTED) {
  delay(500);
  Serial.print(".");
}
Serial.println("\n Connectat!");
Serial.print("IP: ");
Serial.println(WiFi.localIP());

